<?xml version="1.0" encoding="UTF-8"?>
<!-- Structured JSON console logs with a per-request traceId (from TraceIdFilter MDC), §8.5. -->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>{"ts":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}","level":"%level","traceId":"%X{traceId:-}","logger":"%logger{36}","thread":"%thread","msg":"%replace(%msg){'"','\\"'}","exception":"%replace(%replace(%xException){'\n','\\n'}){'"','\\"'}"}%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.goalkeeperdash" level="INFO"/>
</configuration>
